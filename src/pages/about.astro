---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { SceneManager } from "@/components/about/SceneManager";
import { BRANCHES } from "@/components/about/SceneConfig";
---

<BaseLayout title="About - University of Michigan Mars Rover Team">
  <div class="about-content-wrapper">
    <SceneManager client:only="react" />

    <main class="relative z-10">
      {BRANCHES.map((branch) => (
        <div
          class="branch-wrapper"
          data-branch={branch.name}
          style={`--branch-sections: ${branch.sections.length}; --branch-color: ${branch.color}; --branch-accent: ${branch.accent};`}
        >
          <div class="branch-inner">
            <div class="branch-slides">
              {branch.sections.map((section) => (
                <section class="slide" data-id={section.name}>
                  {section.name === 'mission-intro' ? (
                    <div class="slide-intro">
                      <h1 class="intro-title">
                        {(section.label || '').split(' ').map((word: string, wordIndex: number) => (
                          <div class="word" style={`--word-index: ${wordIndex}`}>
                            {word.split('').map((char: string, charIndex: number) => (
                              <span class="char" style={`--char-index: ${charIndex}; --word-index: ${wordIndex}`}>
                                {char}
                              </span>
                            ))}
                          </div>
                        ))}
                      </h1>
                    </div>
                  ) : section.name === 'mission' ? (
                    <div class="card">
                      <h2 class="card-title">Our Mission</h2>
                      <p class="card-desc">{section.description}</p>
                    </div>
                  ) : (
                    <div class="card">
                      <h2 class="card-title">{section.subteam?.name}</h2>
                      <p class="card-desc">{section.subteam?.desc}</p>
                    </div>
                  )}
                </section>
              ))}
            </div>
          </div>
        </div>
      ))}
    </main>

    <script>
      function clamp(min: number, input: number, max: number) {
        return Math.max(min, Math.min(input, max))
      }

      function lerp(start: number, end: number, factor: number) {
        return start + (end - start) * factor
      }

      document.querySelectorAll('.branch-wrapper').forEach((wrapper) => {
        const slides = wrapper.querySelector('.branch-slides') as HTMLElement
        const sections = slides?.querySelectorAll('.slide') as NodeListOf<HTMLElement>

        if (!slides || !sections || sections.length <= 1) return

        let currentX = 0
        let targetX = 0
        const damping = 0.1

        const updateTarget = () => {
          const rect = wrapper.getBoundingClientRect()
          const wrapperHeight = wrapper.clientHeight
          const vh = window.visualViewport?.height || window.innerHeight

          const scrolledIntoWrapper = -rect.top
          const scrollableDistance = wrapperHeight - vh

          let progress = scrolledIntoWrapper / scrollableDistance
          progress = clamp(0, progress, 1)

          const slidesWidth = slides.scrollWidth
          const maxX = slidesWidth - window.innerWidth
          targetX = progress * maxX
        }

        const animate = () => {
          currentX = lerp(currentX, targetX, damping)
          slides.style.transform = `translateX(${-currentX}px)`
          requestAnimationFrame(animate)
        }

        window.addEventListener('scroll', updateTarget, { passive: true })
        window.addEventListener('resize', updateTarget)
        window.visualViewport?.addEventListener('resize', updateTarget)
        updateTarget()
        animate()
      })
    </script>
  </div>
</BaseLayout>

<style>
  @import url('https://api.fontshare.com/v2/css?f[]=panchang@400,500,600,700&display=swap');
  @import url('https://fonts.googleapis.com/css2?family=Rajdhani:wght@300;400;500;600;700&display=swap');

  .about-content-wrapper {
    position: relative;
    clip-path: inset(0);
    font-family: 'Rajdhani', system-ui, -apple-system, sans-serif;
  }

  :global(footer) {
    display: none;
  }

  :global(header) {
    position: fixed !important;
    top: 0;
    left: 0;
    width: 100%;
    background: transparent !important;
    border: none !important;
    box-shadow: none !important;
    backdrop-filter: none !important;
  }

  main {
    position: relative;
    z-index: 1;
    color: white;
  }

  .branch-wrapper {
    height: calc(var(--branch-sections) * 100dvh);
    position: relative;
  }

  .branch-inner {
    position: sticky;
    top: 0;
    height: 100dvh;
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  .branch-slides {
    display: flex;
    flex: 1;
    align-items: stretch;
  }

  .slide {
    min-width: 100vw;
    width: 100vw;
    height: 100dvh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: flex-end;
    padding: 0 4rem 4rem 4rem;
    box-sizing: border-box;
    pointer-events: none;
    flex-shrink: 0;
  }

  .slide[data-id="mission-intro"] {
    justify-content: center;
    align-items: center;
  }

  .slide-intro {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .intro-title {
    font-family: 'Panchang', sans-serif;
    font-size: clamp(2.5rem, 12vw, 8rem);
    font-weight: 700;
    text-transform: uppercase;
    color: #ffffff;
    margin: 0;
    line-height: 1;
    letter-spacing: 0.02em;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    user-select: none;
  }

  .intro-title .word {
    display: block;
    overflow: hidden;
  }

  .intro-title .char {
    display: inline-block;
    opacity: 0;
    transform: translateY(100%) rotateX(-90deg);
    animation: char-reveal 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: calc(0.4s + var(--word-index) * 0.3s + var(--char-index) * 0.05s);
  }

  @keyframes char-reveal {
    0% {
      opacity: 0;
      transform: translateY(100%) rotateX(-90deg);
    }
    100% {
      opacity: 1;
      transform: translateY(0) rotateX(0deg);
    }
  }

  .card {
    width: 500px;
    min-height: 180px;
    padding: 1.5rem;
    background: rgba(14, 14, 14, 0.5);
    backdrop-filter: blur(8px);
    border: 1px solid var(--branch-accent, rgba(255,255,255,0.3));
    color: white;
    pointer-events: auto;
    display: flex;
    flex-direction: column;
  }

  .card-title {
    font-family: 'Panchang', sans-serif;
    font-size: 1.75rem;
    font-weight: 600;
    text-transform: uppercase;
    line-height: 1.2;
    color: var(--branch-accent, #FF8C00);
    margin: 0 0 1rem 0;
    word-break: keep-all;
  }

  .card-desc {
    font-family: 'Rajdhani', sans-serif;
    font-size: 1.25rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
  }

  :global(html),
  :global(body) {
    overscroll-behavior: none;
    -webkit-overflow-scrolling: auto;
  }

  :global(body) {
    touch-action: pan-x pan-y;
  }

  @media (max-width: 800px) {
    .slide {
      padding: 0 1.5rem 2rem 1.5rem;
      align-items: flex-start;
    }

    .card {
      width: calc(100vw - 3rem);
      max-width: none;
    }

    .card-title {
      font-size: 1.4rem;
    }

    .card-desc {
      font-size: 1.1rem;
    }
  }
</style>
